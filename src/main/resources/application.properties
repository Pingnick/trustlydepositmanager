# --------------------------------------------------------
# Logging:
# --------------------------------------------------------
logging.level.root=INFO
logging.level.se.test.trustlydepositmanager=DEBUG

# Logging pattern for the console
logging.pattern.console= %d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Logging pattern for file
logging.pattern.file= %d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n


# --------------------------------------------------------
# Database Configuration:
# --------------------------------------------------------
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.username=sa
spring.datasource.password=password
spring.datasource.initialization-mode=never
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.hibernate.show-sql=true
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy
spring.h2.console.enabled=true

# --------------------------------------------------------
# Application Settings:
# --------------------------------------------------------
tdm.api.security.private_key.path=src/main/resources/keys/our_private_key.pem
tdm.api.security.public_key.path=src/main/resources/keys/trustly_test_public_key.pem
tdm.api.security.username=devcode
tdm.api.security.password=change_this_gef4D37ypL

tdm.api.trustly.url=https://test.trustly.com/api/1
tdm.api.notification.url=https://localhost:8080/api/notifications

# --------------------------------------------------------
# --------------------------------------------------------